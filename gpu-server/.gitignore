# ── Model weights (large binaries, manage via DVC / LFS) ──────────────────────
models/*.pth
models/*.pt
models/*.bin
models/*.onnx
models/*.safetensors
models/*.gguf
models/disaster_model/

# ── Runtime directories ────────────────────────────────────────────────────────
temp/
cache/
logs/
